services:

  web:
    container_name: ikatoo-frontend
    image: nginx:alpine
    working_dir: /usr/share/nginx/html
    volumes:
      - ./frontend-dist:/usr/share/nginx/html:ro
    ports:
      - ${PORT}:80

